#include <stdio.h>
#include <string.h>

/**
 * _strcat - concatenate two stringa
 * @dest : receives destination
 * @src : receives source
 * Return: a pointer
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
