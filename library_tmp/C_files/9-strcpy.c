#include <string.h>
#include <stdio.h>
/**
 * _strcpy - function to print an array
 * Return: char
 * @dest: dest pointer
 * @src: src pointer
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
