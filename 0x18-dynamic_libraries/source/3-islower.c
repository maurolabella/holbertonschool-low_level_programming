#include "main.h"
#include<ctype.h>
/**
 * _islower - detect lowercases
 * Return: algo
 * @c : receives a character to be controlled
*/
int _islower(int c)
{
if (islower(c))
{
return (1);
}
else
{
return (0);
}
}
