#include"main.h"
#include<ctype.h>


/**
 * _isupper - returns 1 if int is uppercase
 * @c : receives an integer
 * Return: 1 if uppercase
 */
int _isupper(int c)
{
if (isupper(c) == 0)
{
return (0);
}
else
{
return (1);
}
}
