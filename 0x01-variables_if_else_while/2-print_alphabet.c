#include<stdio.h>
#include<time.h>
#include<stdlib.h>

/**
 * main - main
 * Return: algo
 */
int main(void)

{

int ch;
char sl = '\n';

for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch);
}
putchar(sl);

return (0);

}
