#include"main.h"
#include<ctype.h>


/**
 * mul - returns the product of two numbers
 * @a : receives an integer
 * @b : receives an integer
 * Return: 0 otherwise
 */
int mul(int a, int b)
{
return (a * b);
}
