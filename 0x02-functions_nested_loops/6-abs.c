#include"main.h"
#include<stdlib.h>
#include<ctype.h>

/**
 * _abs - return the abs. value of a number
 * @n : receives a number to be processed
 * Return: 0
**/
int _abs(int n)
{
int m = abs(n);
return (m);
}
