#include <string.h>
#include <stdio.h>
#include<stdlib.h>
#include "main.h"
/**
 * _atoi - filter and convert a string in a integer
 * Return: an integer
 * @s : a string is received
 */
int _atoi(char *s)
{
	return (atoi(s));
}

